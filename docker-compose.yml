services:
  piano:
    build: .
    ports:
      - "4444:8080"
    volumes:
      - piano_data:/app/data
      - piano_backups:/backups
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1

volumes:
  piano_data:
  piano_backups:
